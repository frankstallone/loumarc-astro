---
import Head from '../components/Head.astro';
import Footer from '../components/Footer.astro';

const { meta, disableFooter } = Astro.props;
---

<!DOCTYPE html>
<html lang="en">
  <Head meta={meta} />
  <body>
    <slot />
    {disableFooter ? undefined : <Footer />}
  </body>
</html>
