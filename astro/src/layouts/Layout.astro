---
import Head from "../components/Head.astro";
import Footer from "../components/Footer.astro";

const { meta, slug } = Astro.props;
---

<!DOCTYPE html>
<html lang="en">
  <Head meta={meta} />
  <body>
    <slot />
    {slug === "thank-you" ? null : <Footer />}
  </body>
</html>
